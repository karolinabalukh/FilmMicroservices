# 🎬 Film Microservice

Мікросервісна система для керування фільмами з асинхронною відправкою email-повідомлень через Kafka  
та логуванням результатів у Elasticsearch з переглядом через Kibana.

---

Проєкт складається з таких компонентів:

- **Film Service** — REST API для створення фільмів
- **Email Service** — Kafka consumer, який відправляє email та зберігає статус
- **Kafka** — брокер повідомлень
- **Elasticsearch** — зберігання статусів відправки email
- **Kibana** — перегляд логів та статусів
- **Docker Compose** — оркестрація всієї системи

---

## Інструкція із запуску

### Збірка JAR-файлів

У корені проєкту (де знаходяться обидва сервіси) виконай команду:

```bash
mvn clean package -DskipTests 
```

## Налаштування конфіденційних даних

Створіть файл **`.env`** у корені проєкту  
(поруч із `docker-compose.yml`) та додайте SMTP-налаштування:

```env
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your_email@gmail.com
MAIL_PASSWORD=your_app_password
```


## Запуск всієї системи

Підняти всі контейнери  
(**Kafka, Elasticsearch, Kibana, Film Service, Email Service**):

```bash
docker-compose up -d 
```

## Доступ до сервісів

| Сервіс | URL |
|-------|-----|
| Film Service API | http://localhost:8082 |
| Kibana | http://localhost:5601 |
| Elasticsearch | http://localhost:9200/emails/_search?pretty |

